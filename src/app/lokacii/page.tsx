"use client";
import React from 'react'
import { getBanners } from '../../../sanity/sanity-utils';
import { Banner } from '../../../types/Banner';

export default async function Lokacii() {
    const banners = await getBanners();
return (
<div>
    Asdasdasdad
</div>)
}